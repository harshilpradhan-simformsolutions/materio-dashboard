import { Tag } from 'antd';
import styled from 'styled-components';

export const TagBase = styled(Tag)`
  &.ant-tag {
    font-size: 12px;
    font-weight: 500;
    line-height: 16px;
    padding: 4px 8px;
  }
`;
